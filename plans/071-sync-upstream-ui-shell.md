# Sync upstream UI shell

- [ ] Add a plan document for the UI sync
- [ ] Sync shared frontend shell styling with upstream layouts, app shell, and login page
- [ ] Restore upstream forward-page styling while preserving TCP preconnection controls and payloads
- [ ] Update monitor/dashboard styling to match upstream without changing core behavior
- [ ] Run frontend lint and build, then review the final diff for unrelated changes
