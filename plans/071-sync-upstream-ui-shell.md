# Sync upstream UI shell

- [x] Add a plan document for the UI sync
- [x] Sync shared frontend shell styling with upstream layouts, app shell, and login page
- [x] Restore upstream forward-page styling while preserving TCP preconnection controls and payloads
- [x] Update monitor/dashboard styling to match upstream without changing core behavior
- [x] Run frontend lint and build, then review the final diff for unrelated changes
